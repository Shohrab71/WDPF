


<?

	// $days = array("Sunday","Monday","Tuesday");

	// //echo is_array($days);  
	// //1 means true

	// $str = "Hello";

	// //echo is_array($str);
	// // nothing means false

	// echo "<h1>is_bool</h1>";

	// is_bool($days);

	// $bool = true;

	// printf("the value of  \$bool is = %d",$bool);
?>
	<<!-- h1> is_float()</h1>

<?
	
	// $num = 35.2;
	// // $num = 35;
	// $check = is_float($num);
	// printf("the value of  \$check is = %d",$check);
?>
 
 is_array(), is_bool(), is_float(), is_integer(), is_null(), is_numeric(),
is_object(), is_resource(), is_scalar(), and is_string().